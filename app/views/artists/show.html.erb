<p id="notice"><%= notice %></p>

<h1><%= @artist.name %></h1>

<%= link_to 'Edit', edit_artist_path(@artist) %> |
<%= link_to 'Back', artists_path %>

<hr>

<h2>Albums</h2>

<ol>
<% @artist.albums.each do |album| %>

  <li><%= link_to album.title, album_path(:id=>album.id) %></li>

<% end %>
</ol>
