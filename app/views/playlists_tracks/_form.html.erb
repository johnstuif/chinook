<%= form_for(@playlists_track) do |f| %>
  <% if @playlists_track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@playlists_track.errors.count, "error") %> prohibited this playlists_track from being saved:</h2>

      <ul>
      <% @playlists_track.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :playlist_id %><br>
    <%= f.number_field :playlist_id %>
  </div>
  <div class="field">
    <%= f.label :track_id %><br>
    <%= f.text_field :track_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
